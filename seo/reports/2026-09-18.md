# SEO Routine Report — 2026-09-18

Second bi-weekly run. Compared against the 2026-09-04 report throughout.

Data windows: GSC current = 2026-09-02..09-15, prior = 2026-08-19..09-01 (14-day windows, ending 3 days before today per GSC's reporting lag). GA4 = last 28 days (2026-08-21..09-17) split into two 14-day halves myself, since the script only takes `--days`; note several days in each half have zero rows and are simply absent from the API response (12/14 days present in the prior half, 11/14 in the current half) — treat the session counts as directionally useful, not exact. Glyphex = trailing 30 days (its own fixed window). Ahrefs = live snapshot at time of check (no fixed trailing window; compared against the 09-04 report's figures). PSI = single live run per strategy, compared to the 09-04 lab run.

Per the routine's noise threshold: any week-over-week move under ~30% is called out as noise below, since GSC volumes here are still small (< 2,000 impressions/window, <60 clicks/window).

## Headline numbers

| Metric | Current | Prior | Delta |
|---|---|---|---|
| GSC clicks | 18 | 23 | -5 (-21.7%, **noise**) |
| GSC impressions | 1,668 | 1,618 | +50 (+3.1%, **noise**) |
| GSC CTR | 1.08% | 1.42% | -0.34 pt (**noise** at this volume) |
| GSC avg position | 10.40 | 14.20 | **+3.8 better** (real — driven mostly by `/best-practices/transportation-safety/`) |
| GA4 organic sessions (14d halves) | 47 | 30 | +17 (+56.7%, real move, but see data-completeness caveat above) |
| Glyphex Organic Search visitors (30d trend) | 52 | — | -15% over the trailing 30 days (disagrees with GA4 direction — see Glyphex section) |
| Ahrefs referring domains (live) | 183 | 141 | +42 (almost all spam — see below) |
| Ahrefs DR | 7 | 6 | +1 |
| Ahrefs Health Score | 97 | n/a (not captured 09-04) | — |
| Homepage PSI mobile perf / LCP | 70 / 5.4s | 71 / 5.4s | flat — the 09-04 LCP preload fix shipped but produced **zero measurable change** |
| Homepage PSI desktop perf / LCP | 95 / 0.8s | 97 / 0.8s | flat |

GSC and GA4/Glyphex disagree on organic direction again this cycle: GSC clicks -21.7%, GA4 sessions +56.7%, Glyphex organic -15% (30-day trend, not the same window). At this traffic volume (tens of sessions/clicks per window), single days and differing attribution windows/definitions (GSC = Google-only clicks with a 2-3 day lag; GA4 = session-based, all engines; Glyphex = its own first-party model) can easily swing in different directions — not treating this as a real signal either way. One consistent thread across all three: `/best-practices/transportation-safety/` and `/press/birdcast-integration/` are both up sharply in GSC impressions and Glyphex visitors respectively, so that part of the story agrees.

## New issues

Ranked by impact.

1. **Spam backlink network has roughly quadrupled in two weeks.** 09-04 report flagged ~35-41 spam domains (mostly `.shop`) as "growing, don't disavow yet." Today: 183 total referring domains, of which **166 are Ahrefs-flagged SPAM** (91%) — up from ~41 at last check. At least 25 of those are newly first-seen since 09-04 (e.g. `seopxl-performance-authority-engine.shop`, `seo-growth-authority-boost-hub.shop`, `bitproperty.online`, `eliteseo.site`, `getranked.site`, `linkrank.shop`), same `-rank-/-seo-/-link-` naming template as before, joined by a new sub-pattern of decoy domains claiming 300M+ "dofollow linked domains" from a single referring domain (`betulcrime.com`, `bitproperty.online`, `homesforsaleoldgreenwichct.com`, `kkinsider.com`, `theforbestimes.com`, `uncledspizza.com`) — a classic link-farm/PBN fingerprint. DR is still only 7 (+1 in two weeks) and organic performance shows no negative correlation, so Google appears to be discounting these, but the growth rate crossed the threshold the last report set for reconsidering disavow. High.
2. **The 09-04 homepage LCP fix (hero poster preload + `preload="metadata"`) shipped but had no measurable effect.** Mobile Lighthouse performance 71→70, LCP 5.4s→5.4s exactly. Either the preload tag isn't reaching the rendered HTML as intended, isn't the actual LCP element, or is being overridden by something else in the critical path — worth inspecting the live rendered HTML directly rather than assuming the fix landed correctly just because the commit shipped. High.
3. **`beta.photometrics.ai` still earning GSC impressions, and a new URL variant appeared.** 4 beta URLs this window (`/`, `/ViewScheduleMap` — new, wasn't seen before, `/ViewStreetLightMap`, `/ViewTargetLightingMap`); prior window also had 4 but different set (`/`, `/Home`, `/MapTestPage`, `/ViewStreetLightMap`, `/ViewTargetLightingMap`). The GSC Removals action item from 09-04 evidently wasn't submitted, or was submitted but isn't effective against a dead host serving new URL variants each crawl. High.
4. **The 5 best-practices pages carry no `Article` schema or visible byline despite `og:type: article`.** `layouts/partials/head.html` only fires `schema-article.html` for the `insights` section — `birds`, `dark-sky`, `public-safety`, `transportation-safety`, `utilities` are genuine original editorial content with no Article JSON-LD, no `article:published_time`, no on-page byline. Largest E-E-A-T/schema gap on the site; straightforward template fix. High/Medium.
5. **Broken image reference in `/about/`'s Person schema.** `layouts/partials/schema-person.html:10` sets `"image": "{{ .Site.BaseURL }}images/ari-isaak.jpg"` — confirmed via `ls static/images/` that this file does not exist (404). Broken structured data can disqualify the Person entity from rich-result/Knowledge Graph eligibility. Medium.
6. **`llms.txt` is stale.** Missing 3 of 8 sitemap insights pages (`crashes-after-dark-map`, `lowest-liability-overlighting`, `phase-calculator-guide`) and the `dark-sky` best-practices page. ~10 minute fix. Medium.
7. **`/faq/`'s 22 questions have zero heading markup.** All render as plain `<button>` elements (`layouts/faq/single.html:17-22`) with no H2/H3 per question — for a page whose entire value proposition is long-tail/PAA targeting, this is a real structural gap for featured-snippet eligibility. Medium.
8. **`/tools/`'s `ItemList` schema is stale.** Lists only 3 `SoftwareApplication` items but the live page has a 4th tab ("Safety & Lighting") unrepresented in schema; all 3 listed items share the identical generic `/tools/` URL instead of the tab-specific anchors (`#twilight`, `#phase`, `#gissearch`, `#safety`) the page's own JS already supports. Medium.
9. **`content/insights/adaptive-street-lighting.md` remains thin and stale** — 376 words vs. 1,000-2,100 for sibling insights pieces, dated 2026-03-14 (oldest-feeling piece in that section), and unchanged since the 09-04 report flagged it as "the top content item" for the "adaptive street lighting" SERP (where the site holds ~pos 9-10 but can't break into page 1 with 432-word content). Medium — carried forward and escalating since nothing changed in two weeks.
10. **AI Overview citation gap.** The "street light power consumption calculation" AI Overview (this run's SERP check) cites `tu-bu.com` and `zgsm...` (Chinese LED manufacturer content), not photometrics.ai, despite photometrics.ai ranking organically (~pos 7, via the transportation-safety insights piece) for the same query. No dedicated calculator page exists to compete for the citation. Info/Medium — ties to the GEO section's broader finding that AI-platform citation correlates more with Reddit/YouTube/Wikidata presence than with search rank alone.
11. **Title tags too long on both rotation pages checked this cycle:** `/faq/` title ~77 chars ("Frequently Asked Questions | Photometrics AI | Street Lighting Optimization"), meta description ~215 chars (both will truncate); `/tools/` title ~76 chars ("Streetlighting Data Tools | Photometrics AI | Street Lighting Optimization"). Low-Medium.
12. Sitemap `lastmod` values are stale/inaccurate on several pages (`how-it-works/` shows 2026-03-13 but was last committed 2026-09-04; `birds/` and `public-safety/` show 2026-03-13 vs. actual 2026-04-11) — front-matter override is out of sync with real edits. Low.
13. Redirect-chain hygiene: `http://www.photometrics.ai/` takes 2 hops to reach the canonical apex. Low.
14. Homepage hero video has no `VideoObject` schema; two disconnected Person nodes for Ari Isaak (Organization `founders` vs. the fuller About-page Person). Low.

## Still open

Carried from 2026-09-04, unresolved:
- `beta.photometrics.ai` GSC impressions — see New Issue #3 above (escalated: new URL variant appeared, original fix action apparently never completed).
- Spam link network — see New Issue #1 above (escalated: quadrupled, was "watch and re-check" last time).
- No IndexNow key file at site root.
- `/how-it-works/` H1 and title are still generic ("How It Works") — the recommended rewrite ("How Photometrics AI Optimizes Street Lighting") from 09-04 was not applied (benefits' equivalent rewrite was).
- Homepage word count / indexable vignette captions — not independently reverified this cycle (homepage wasn't in this run's page-rotation or content-quality checks); carrying forward as open until next homepage-specific pass.

## Resolved

From 2026-09-04, confirmed fixed via commit `9c96732`:
- Benefits page footnotes re-pointed from S3 archive links to primary publisher URLs.
- Benefits H1/title rewritten from "The Numbers" to "Street Lighting Optimization ROI."
- `how-it-works-system.png` converted to WebP with explicit width/height and `loading="lazy"`.
- `og-image` reduced from 503 KB PNG to a 36 KB JPEG (confirmed via `static/images/og-image.jpg` file size).

Attempted but **not** resolved (see New Issue #2): the homepage hero LCP preload fix shipped but produced no measurable Lighthouse improvement.

## Weakest pages (rotation: about, faq, tools)

| Page | Score | Top issue |
|---|---|---|
| `/tools/` | 67 | Stale `ItemList` schema (missing 4th tab, generic URLs); thin prose; substantial content is JS-only with no server-rendered fallback |
| `/faq/` | 74 | Zero heading markup across 22 questions; title (~77c) and meta description (~215c) both too long |
| `/about/` | 78 | Broken Person schema `image` (404); generic H1 ("About"); zero contextual internal links in body copy |

## Action items

**High**
- Investigate why the 09-04 LCP preload fix had no effect — check the live rendered HTML on `/` for whether the preload tag/`fetchpriority` actually shipped and targets the true LCP element. `layouts/index.html` or hero partial.
- Re-submit GSC Removals for all current `beta.photometrics.ai` URLs (`/`, `/ViewScheduleMap`, `/ViewStreetLightMap`, `/ViewTargetLightingMap`) — manual, GSC UI.
- Decide on disavow for the spam backlink network now that it's at 166 flagged domains (91% of all referring domains) — export the full list via Ahrefs (`Export` button on Referring domains report) for review.
- Add `Article` schema + byline to the 5 best-practices pages, reusing `schema-article.html`. `layouts/partials/head.html`.

**Medium**
- Fix the broken Person schema `image` field — add `/images/ari-isaak.jpg` or repoint to an existing headshot asset. `layouts/partials/schema-person.html:10`.
- Update `llms.txt` to add `crashes-after-dark-map`, `lowest-liability-overlighting`, `phase-calculator-guide`, and `dark-sky`.
- Wrap each FAQ question in an H2/H3. `layouts/faq/single.html:17-22`.
- Shorten `/faq/` title tag and meta description (front matter).
- Update `/tools/` `ItemList` schema: add the Safety & Lighting item, use tab-specific anchor URLs. `layouts/partials/schema-tools.html`.
- Expand `content/insights/adaptive-street-lighting.md` from 376 words toward the 1,000+ word range of its siblings — still the top content gap for the "adaptive street lighting" SERP.
- Rewrite `/how-it-works/` H1/title (carried up from Low last cycle — two runs unaddressed).

**Low**
- Fix stale sitemap `lastmod` values — derive from Hugo `.GitInfo` rather than a manual front-matter override.
- Collapse the `http://www.` → apex redirect to a single hop (Amplify `customRules`).
- Add an IndexNow key file and ping on publish.
- Add `VideoObject` schema to the homepage hero video; link the two Ari Isaak Person nodes via a shared `@id`.

## Content prompts

- **New GSC queries this window** (impressions ≥1, filtered for signal): `precision based lighting`, `dark sky api`, `lighting photometric`, `adaptive street lighting system`, `intersection lighting`, `site photometric plan`, `streetlight asset management`, `dark sky compliance`, `darksky api`, `photometrics software`.
- **High-impression/zero-CTR queries** (title/meta-rewrite or new-content candidates): `photometrics` (207 impr, pos 6.5, 0 clicks — brand query still converting 0%, worth a SERP check of its own), `street light power consumption calculation` (28 impr, pos 10, 0 clicks — no dedicated calculator page exists), `photometrics lighting` (26 impr), `lighting photometrics` (23 impr, pos 41.8), `photometric calculation` (20 impr, pos 50.1), `streetlight maintenance savings` (19 impr, pos 11.4).
- **AI Overview citation gap**: see New Issue #10 — a dedicated calculator/tool page for street light power consumption could compete for both the organic ranking and the AI Overview citation currently going to Chinese LED manufacturer sites. Glyphex confirms real (if small) AI-assistant referral traffic already exists (3 visitors from Gemini + ChatGPT over 30 days), so this isn't a hypothetical channel.
- **SERP check — "street light power consumption calculation"**: top 8 organic results dominated by LED manufacturer calculators (WOSEN, Sinocoled, elitesemicon — all Chinese manufacturer content) and by competitor FLASHNET's inteliLIGHT energy-savings calculator; photometrics.ai's transportation-safety insights piece ranks ~7th. No PAA box appeared. "People also search for": street light power consumption calculation excel, typical LED street light wattage, solar street light design calculation manual pdf, intelligent street lighting system.
- **Competitor checked (Ubersuggest): Ubicquia** (`ubicquia.com`) — DA 38, 110 organic keywords, 452 organic visits/mo, 141K backlinks. Top keywords are entirely branded (`ubicquia`, `ubicell`, `axon lightpost`) — no meaningful organic keyword overlap with Photometrics AI found. Low near-term SEO competitive threat; per the competitive-intel skill, Ubicquia is a Category 2/3 complementary-partner candidate (UbiVu operational analytics), not a direct competitor.
- **Real referring domains worth nurturing** (updated): `scoop.it` (DR82), `dataforseo.com` (DR76, new), `datainnovation.org` (DR75), `birdcast.org` (DR51), `lightnowblog.com` (DR50), `eventship.com` (DR49, new), `stopbirdcollisions.org` (DR49), `geomusings.com` (DR43), `chicagobirder.org` (DR40), `uslightingtrends.com` (DR34), `birdingwire.com` (DR28), `darkskypa.org` (DR7, new — a dark-sky advocacy org, worth an outreach look).
- **Glyphex signals** (30-day trend): `/press/birdcast-integration/` visitors +192%, `/best-practices/transportation-safety/` +171% — both worth an internal-link/share push per Glyphex's own "Page on the rise" insight. `/take-action/` shows real multi-page engagement (32 visitors, 88 views, ~2.75 pages/visitor) but also logged a rage-click on the `#ta-generate` button — worth a UX glance next time someone's in that flow. Referral traffic +194% and a France visitor spike (+100%, 11% of total) are both unexplained — worth a source breakdown next run.

## Skipped

- Ahrefs "New"/"Lost" history filters remain locked on the free plan (same limitation as 09-04); spam-domain growth and "new" classification inferred from First-seen dates instead.
- GA4 per-window landing-page split wasn't available — `ga4_report.py --report top-pages` only returns an aggregate over the full `--days` window, not split by date range. Reported the 28-day aggregate top pages instead and skipped the "landing page dropped >25%" check, since per-page session counts are single digits at this traffic volume and not statistically meaningful.
- `crux`/`crux-history` skipped per instructions — no CrUX field data for this origin (insufficient Chrome traffic).
- Both Glyphex and Ahrefs required a fresh sign-in in the automation browser tab this run — neither session carried over from any other browser context. Resolved mid-run once Ari signed in directly in the automation tabs (Glyphex needed the Google button specifically; a magic-link attempt hit an expired-token error, consistent with the known "magic link opens elsewhere" failure mode). Recurring friction point across both 09-04 and this run — worth keeping in mind for next time.

## Rotation state

- Pages checked this run: `about`, `faq`, `tools`. Next: `take-action`, `insights/adaptive-street-lighting`, `insights/beyond-led-conversion`.
- Ubersuggest competitor checked: Ubicquia (`ubicquia.com`). Next: Schréder (`schreder.com`).
- SERP/PAA topic used: "street light power consumption calculation". Next: "predictive lighting" (surfaced this run as a new/rising GSC query, 9 impressions, pos 19.3).
- Geo/audit (every-other-run deep check): run this cycle (opportunistically, since the 09-04 report was the first-ever run and didn't include it). Next due in ~2 cycles (skip 2026-10-02, run again 2026-10-16).
